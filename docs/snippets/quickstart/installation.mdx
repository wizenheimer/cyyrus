## Install Cyyrus

Install Cyyrus with your preferred package manager:

<CodeGroup>
```bash pip
# Cyyrus requires Python 3.11 or greater
pip install -U cyyrus
```

```bash uv
# Cyyrus requires Python 3.11 or greater
uv pip install -U cyyrus --prerelease=allow
```
</CodeGroup>